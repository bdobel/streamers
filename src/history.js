// history package installed automatically with react-router-dom as a dependcency
// creates history obj that can be accessed from anywhere in project
import { createBrowserHistory } from 'history'; 
export default createBrowserHistory();